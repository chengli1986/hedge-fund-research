# GMIA — Global Market Insight Aggregator

Tracks and aggregates research insights, market commentary, and papers from top hedge funds. Summarizes via LLM and publishes a bilingual (CN/EN) dashboard.

## Sources (40)

| Fund | Method | Frequency | Notable |
|------|--------|-----------|---------|
| **Man Group** | SSR (requests) | Weekly | Macro, quant, systematic trading |
| **Bridgewater Associates** | SSR (requests) | Monthly | Macro, risk parity, All Weather — full content + LLM analysis |
| **AQR Capital** | SSR (requests) | Monthly | Factor investing, quantitative research; Citrix WAF blocks EC2 IPs intermittently — nightly test auto-skips when 0 articles |
| **GMO LLC** | JSON API | Quarterly | Value contrarian, 7-Year forecasts |
| **Oaktree Capital** | Playwright (CSR) | Monthly | Howard Marks memos, credit/distressed |
| **ARK Invest** | RSS feed | Weekly | Analyst Research, Market Commentary |
| **Cambridge Associates** | SSR (requests) | Monthly | Private equity, venture capital, private credit |
| **Wellington Management** | Playwright (AEM) | Weekly | Equity, macro, fixed income, multi-asset, ESG |
| **Amundi Research Center** | RSS | Weekly | Macro, ESG, emerging markets, fixed income |
| **T. Rowe Price** | Playwright (AEM) | Weekly | Equity, fixed income, active management |
| **PIMCO** | Playwright (Coveo) | Weekly | World's largest fixed-income manager; macro, credit, secular outlook |
| **Aberdeen Investments** | Playwright (Next.js) | Weekly | EM debt, multi-asset, sustainable investing |
| **Brookfield Asset Management** | SSR (Drupal) | Monthly | Real assets, infrastructure, renewable power, private equity |
| **J.P. Morgan Asset Management** | AEM JSON API | Weekly | Multi-asset, fixed income, market insights, Long-Term Capital Market Assumptions |
| **Verdad Capital** | RSS feed (newsletter) | Weekly | Quant value/factor, emerging markets, private equity replication; Boston, ~$1B. Read from the Mailchimp campaign archive — the Squarespace site stopped mirroring the weekly after 2026-05-04 while the newsletter kept shipping (see `fetch_verdad`) |
| **MSCI Research** | Playwright (Next.js) | Weekly | Index/factor research, ESG & climate frameworks, multi-asset risk; $18T+ benchmarked |
| **Natixis Investment Managers** | SSR (requests) | Monthly | Tactical Take podcast — macro/portfolio strategy with Janasiewicz & Hess |
| **Apollo Global Management** | SSR (AEM) | Weekly | Private credit ABF, secondaries, View from Apollo, Apollo Academy |
| **KKR** | Playwright (Next.js) | Weekly | Private equity / infrastructure / private credit / Global Atlantic insurance; NYC, 1976, ~$758B |
| **Janus Henderson Investors** | SSR (requests) | Weekly | Equities / fixed income / multi-asset; global rates + geopolitics frame; London/Denver, 2017 merger, ~$480B (take-private pending) |
| **Syzygy Asset Management** (ex-Research Affiliates) | Playwright (Next.js) | Monthly | Quantitative research / index licensor; RAFI fundamental indices + Smart Beta; Newport Beach, 2002, ~$159B licensed (Rob Arnott). Renamed 2026-08; researchaffiliates.com redirects to syzygyassetmanagement.com |
| **Goldman Sachs Asset Management** | JSON API | Weekly | Multi-asset, equity, fixed income, alternatives, liquidity; macro outlooks + market commentary; ~$3.3T |
| **Robeco** | Playwright | Weekly | Quant equity pioneer, sustainability/ESG integration, EM equity & global credit; Rotterdam, 1929, ~€228B |
| **D. E. Shaw** | Playwright | Annual | Quant multi-strategy; computational/statistical-arbitrage; Library publishes 1-3 long-form papers/year; 1988, ~$65B |
| **MetLife Investment Management** | SSR (AEM listing JSON + requests) | Weekly | Public fixed income, private credit, real estate, agricultural finance, multi-asset macro; absorbed PineBridge's research teams after completing the acquisition 30 Dec 2025 (replaced the `pinebridge` source on 7-27); moved from `investments.metlife.com` to `www.metlife.com/investments/en-us/` on 7-31 behind a disclaimer cookie gate (8-03 `543cc4d`); Whippany NJ, ~$700B |
| **Ares Management** | SSR (requests) | Weekly | Private credit / private equity / real assets / infrastructure; US direct-lending leader; 1997, NYSE: ARES, ~$644B |
| **Matthews Asia** | SSR (requests) | Monthly | Asia & emerging-markets equity specialist; active bottom-up; SF/HK, 1991, ~$6.6B |
| **Capital Group** | Playwright (AEM) | Weekly | One of the world's largest active managers (~$3.3T); American Funds; 1931; "The Capital System" multi-manager approach; global equity, fixed income, multi-asset |
| **Acadian Asset Management** | Playwright | Monthly | Quantitative/systematic manager (~$120B); Boston, 1986; factor-based equity (valuation/quality/growth/sentiment) across global/EM/frontier markets; systematic credit + multi-asset |
| **Lazard Asset Management** | SSR (requests/AEM) | Weekly | EM equity specialist + macro fixed-income; founded 1848, New York, ~$260B; Lazard Perspective research on geopolitics, EM fundamentals, global monetary policy |
| **Rothschild & Co Asset Management** | SSR (requests) | Weekly | Active boutique within the 200-year-old Rothschild & Co group; Paris, ~€38B; conviction-driven European/thematic equity, multi-asset/macro, credit/fixed income |
| **Goehring & Rozencwajg** | SSR (requests/sitemap) | Weekly | Contrarian natural-resources boutique; energy, precious metals, uranium, agriculture; quarterly commentaries read by 10,000+ professionals; New York, 2015, ~$1.5B |
| **Cohen & Steers** | Playwright | Weekly | Real assets & alternative income specialist; listed REITs/infrastructure pioneer, preferred securities; New York, 1986, ~$94B |
| **Principal Asset Management** | Playwright | Weekly | Global multi-asset manager, asset-management arm of Principal Financial Group; macro, multi-asset, real assets, fixed income; Des Moines, 1879, ~$578B |
| **Franklin Templeton** | Playwright | Weekly | One of the largest independent asset managers (~$1.66T); multi-boutique model — Templeton EM value, Western Asset / Brandywine fixed income, Clarion real estate, Legg Mason + Putnam franchises; San Mateo, 1947 |
| **Partners Group** | SSR (requests) | Monthly | Swiss-listed private-markets specialist (~$186B); thematic transformational investing across private equity, private credit, infrastructure and real estate; pioneered evergreen semi-liquid vehicles; Baar-Zug, 1996 |
| **Blue Owl Capital** | SSR (requests) | Monthly | US alternative asset manager (~$319B) from the 2021 Owl Rock / Dyal Capital merger; three platforms — direct lending ($158B), net-lease real assets incl. data centers ($89B), GP strategic capital ($72B); permanent-capital heavy (>$225B) |
| **Loomis Sayles** | RSS feed | Weekly | Boston fixed-income house, founded 1926 (~$435B as of 6/30/2026); "Alpha Engines" model — autonomous specialist teams over a shared bottom-up credit research bench; multisector/global fixed income, EM debt, munis, plus a concentrated long-horizon growth-equity team |
| **Baillie Gifford** | SSR (requests) | Weekly | Edinburgh growth-equity house founded 1908 (~£197B as of 30 June 2026); the UK's only large-scale independent unlimited-liability investment partnership — no outside shareholders, partners carry the firm's risk personally; long-horizon low-turnover high-conviction portfolios across global/international growth, US growth and EM, plus private positions inside listed vehicles (SpaceX since 2018; once Tesla's largest institutional holder) |
| **Resonanz Capital** | RSS feed | Weekly | Frankfurt-based, BaFin-regulated alternatives specialist founded 2019 (~$6B assets under advisory); builds multi-strategy portfolios from hedge funds, liquid alternatives and open-architecture QIS rather than picking single managers; flagship is the Jazz daily-liquid multi-strategy UCITS, plus bespoke overlay/defensive mandates for institutional allocators |
| **MFS Investment Management** | Solr JSON API | Weekly | Boston active manager founded 1924 (~$645B as of 30 September 2024), a wholly owned Sun Life subsidiary; invented the US open-end mutual fund with Massachusetts Investors Trust and still runs only bottom-up long-horizon active strategies — global/regional equity, fixed income (rates, credit, munis, EM debt) and multi-asset |
| **Northleaf Capital Partners** | SSR (Drupal) | Monthly | Toronto global private-markets manager (~$32B of capital commitments raised), operationally independent since its 2009 TD Bank spin-out and majority-owned since 2020 by Mackenzie Investments (IGM Financial) and Great-West Lifeco; mid-market only, across private equity (secondaries, primaries, co-investments), private credit (direct lending, asset-based specialty finance, NAV lending) and infrastructure |

## Pipeline

```
run_pipeline.sh         — Orchestrator (runs all 4 stages)
  fetch_articles.py     — Stage 1: scrape metadata from all sources, dedup, store JSONL
  fetch_content.py      — Stage 2: download + normalize full article text
  analyze_articles.py   — Stage 3: LLM summarization (CN + EN summaries)
  publish.py            — Stage 4: generate bilingual HTML dashboard
```

## Usage

```bash
# Run full pipeline
bash run_pipeline.sh

# Fetch metadata only
python3 fetch_articles.py

# Fetch single source
python3 fetch_articles.py --source man-group

# Preview without saving
python3 fetch_articles.py --dry-run

# List configured sources
python3 fetch_articles.py --list
```

## Entrypoint Management

Three-layer architecture for resilient research URL management:

1. **Fixed entrypoints** (`config/entrypoints.json`) — verified URLs used for daily fetching
2. **Inspection** — quality metrics in `config/inspection_state.json`, warns on anomalies (consecutive zeros, high gate ratio, domain drift)
3. **Discovery** — `discover_entrypoints.py` scans homepages and scores candidate URLs (domain/path/structure/gate)

**Security**: Content fetcher includes path traversal protection (filename sanitization) and gate detection (paywall/login pages flagged before LLM analysis).

```bash
# Discover new entrypoints (dry-run)
python3 discover_entrypoints.py --source bridgewater

# Write discovered entrypoints to config
python3 discover_entrypoints.py --source bridgewater --write

# Validate existing entrypoints
python3 validate_entrypoints.py
python3 validate_entrypoints.py --source gmo --fix
```

## Candidate Fund Discovery

Automated pipeline for finding and evaluating new hedge fund research sources:

1. **Site discovery** — crawls candidate fund homepages, extracts research links + RSS feeds
2. **Rule-based screening** — detects login walls, paywalls, index-only pages
3. **Entrypoint scoring** — reuses scorer engine with isolated candidate state
4. **LLM deep analysis** — Claude Code agent judges quality (HIGH/MEDIUM/LOW) and GMIA fit
5. **Email report** — HTML summary with color-coded quality/status; includes a dedicated '✅ Trial Passed — Promote?' section for funds awaiting human promotion (separated from the Queue of candidates waiting to start trials)

```bash
# Manual run
bash scripts/wrapper-candidate-discovery.sh

# Seed pool: 5 funds (PIMCO, D.E. Shaw, Blackstone, Two Sigma, KKR)
# Cron: daily at 03:00 BJT (gmia-candidate-discovery)
# Skip logic: 7-day cooldown for analyzed, 30-day for rejected/watchlist
```

## Autoresearch

Scorer weight optimization program using automated experiment loop:

- **Program**: `autoresearch/program.md` — experiment definitions + results log
- **Wrapper**: `scripts/wrapper-autoresearch-gmia.sh` — runs daily at 20:15 BJT via cron
- **History sync**: after each run, `sync-ar-history.py` auto-updates the `autoresearch.html` experiment table on docs.sinostor.com.cn
- **Status**: 7 experiments logged (all at 0.9700 precision — weight tuning ongoing)

## Trial Manager

`gmia-trial-manager.py` — 3-day live trial window for candidate funds. Supports up to 3 concurrent trials (`MAX_CONCURRENT_TRIALS=3`). Runs daily via registered FETCHERS (Playwright/RSS/API — same fetchers as the main pipeline); falls back to httpx for sources without a registered fetcher. Requires articles on ≥2 of 3 days to pass quantity gate. Performs Haiku quality sampling on Day 1, Day 2, and Day 3 (3 articles each, relevance/depth/extractable scores) with cross-day URL dedup so up to 9 distinct articles are scored per trial; slow-updating sources naturally produce smaller samples on later days. Outcomes: APPROVE (add to sources), REJECT (remove from candidates).

## Fetcher Synthesis

`synthesize_fetchers.py` + `fetcher-synthesis/program.md` — weekly Sunday agent that auto-writes Playwright fetchers for `inaccessible` candidates (sites where listing pages are JS-only or selectors are broken), then promotes them back into trial. After `MAX_SYNTHESIS_FAILURES=3` recorded failures in `logs/fetcher-synthesis-history.jsonl`, the candidate is auto-rejected on the next run — bounding wasted agent invocations on candidates the agent cannot solve (e.g. IP/WAF-layer blocks Playwright cannot bypass). Manual override: restore status to `inaccessible` *and* remove the candidate's failed history entries.

## Profile Refresh

`scripts/wrapper-profile-refresh.sh` + `auto-promote/refresh-program.md` — monthly agent that keeps the Sources-tab fund profiles fresh. A headless Claude (Max Plan, `--print`) web-verifies each fund's **time-sensitive** facts (AUM + corporate events: M&A, delisting/take-private, rebrand) and writes a `pending_profiles/<id>.refresh.json` draft only when something changed materially. Static facts (founders, history) are never touched.

Each draft is gated by `validate_refresh()` (reuses the auto-promote evidence gate: every changed fact needs a source URL, AUM magnitude/self-consistency sane, no uncertainty markers; prose rewrites with no corporate-event reason are diff-capped). `scripts/apply_refresh.py` applies a passing draft to the existing `publish._FUND_PROFILES` entry — **change_log is the single source of truth**, so only listed fields change and every other field stays byte-identical; the embedded AUM figure in `config/sources.json` is synced too. `scripts/send_refresh_summary.py` emails an applied/flagged summary (notification only).

```bash
# Manual run (Phase 1 alert-only: gate + email, no write/publish)
ALERT_ONLY=1 bash scripts/wrapper-profile-refresh.sh

# Cron: monthly, 1st at 09:00 BJT (gmia-profile-refresh)
# Phased rollout: ALERT_ONLY=1 (dry-run + email) -> flip to ALERT_ONLY=0 for auto-apply + publish
```

## Tests

1026 passing, 15 deselected in ~10s — unit, functional, and integration tests (live/nightly tests excluded by default via pytest.ini). `TestMetricsWriteCannotKillTheRun` and `TestTheFinalFlushDoesNotHideACrash` (added 9-09 `b2e5361`) close three findings from a manual full-diff review of `b4db49d5..ab3f44bd`, run because the nightly code-review-agent could not chew that range. `record_quality_metrics` is now called from `fetch_source`'s two FAILURE paths as well as its success path, while `fetch_source` itself runs inside a bare `for source in sources:` loop with no try/except — so an OSError from its state write ended the whole run, and since `save_articles(all_new)` comes after that loop, every article already fetched that night was discarded rather than saved. It now never raises, the rule `analyze_articles._append_usage_log` already states for the token log. It also writes through tmp + `os.replace` like every other writer here (`save_articles`, `fetch_content._atomic_write`, and `93971d5`'s backfill fix): a plain `write_text` truncates in place, and a kill mid-write left a partial file that the next run turned into `state = {}` — silently resetting `consecutive_zero_count` for all 42 sources. Separately, `backfill_themes.run()`'s `finally` called `_flush`, whose corpus-changed `SystemExit` REPLACED whatever exception sent it there, so a run that died of something else reported "corpus changed underneath this run"; the refusal stays loud when it is the only failure and steps aside when it is not. An independent review of the two 9-09 changes found the alert whispering at its last hop and its wiring unpinned; both are fixed here. `alerts_subject` now names the zero-fetch condition — it is the only condition that fires while failing/warning/recovered are all empty, so without it the one email this feature exists to produce arrived titled "all OK". `TestTheWiringItself` drives `main()` with probes and SMTP stubbed: four mutants survived the helper-level tests (drop `zero_fetches` from either renderer call, revert `needs_alert` to the old expression — reinstating the exact 09-06 suppression — or compute the list after the `--source` return), and all four are dead now. Two of those assertions were themselves vacuous on the first attempt: `"acadian-asset" in body` also matched the HEALTHY listing, and `"0 articles" in out` is a substring of the healthy row's `"10 articles"`; they assert the section heading instead. `fetch_source` records a zero metric on both early-return paths — a raising fetcher and a `DATE_ORDER_BROKEN` refusal — which previously wrote nothing at all, leaving `last_article_count` showing the previous run's figure; the comment claiming a refusal "shows up as 0 articles, which the health check reports as a FAIL" was wrong on both halves (the return preceded the metric, and the health probe calls the fetcher directly, bypassing the guard). `pipeline_zero_fetches` now requires the id to still be in `sources.json` and the record to be one the last run refreshed (36h): the state file is append-only in practice and still holds `pgim` and `pinebridge` months after retirement. That filter is **anticipated, not observed** — both retired records sit at non-zero counts (`pgim` 8, `pinebridge` 9), so neither would have alerted under the old code; the hazard is that a source retired *because* it stopped producing would be frozen at 0 and could never be cleared. The mirror image is a real gap and is closed too: `pipeline_did_not_run` makes "no configured source was refreshed in 36h" its own send condition, because the freshness filter is right for a retired source and wrong for a dead pipeline — every zero would age out and the check would go quiet exactly when it should be loudest. `gmia_liveness_audit.check_fetch` does not reach back to cover it: whole-pipeline, 4-day threshold, and 4 days is already the largest gap in this repo's fetch history. A failed `send_email` now bumps the exit code — its return was discarded, so an alert that never left the box still reported a healthy night. `record_quality_metrics` counts articles that survived the hostname filter rather than the raw fetcher output, so an entrypoint that drifted to another domain no longer records "10 found" while ingesting 0. `TestFetchAcadianAssetWaitStrategy` (added 9-09) pins `fetch_acadian_asset` to `domcontentloaded`. Measured 9-07: the page reached networkidle at ~29s against the helper's 30s goto timeout — one second of margin — and in a controlled comparison 5 of 6 cold loads produced no cards at all, on the old URL as well as the `?sortby=date-desc` one, so the site is intermittently slow rather than anything that change caused; that is how the 09-06 run fetched 0 articles. Re-measured 9-09 while the site was healthy, the two signals are not equally reliable: cards appear 0.5–1.2s after domcontentloaded on every load, while networkidle ranged 1.3–2.1s that day and 29s two days earlier. Same class as the msci-research and troweprice fixes. Three live fetches after the change returned 10 articles each, and the `date_sorted` guard passing is itself proof the sort parameter still takes effect. `tests/test_pipeline_zero_fetch_alert.py` (added 9-09) makes a silent zero-article fetch reach a human. The pipeline already detected it — `record_quality_metrics` stores `last_article_count` and `consecutive_zero_count`, and `check_anomalies` flags two zeros in a row — but the alert's only destination was `log.warning("ANOMALY ...")` in `logs/fetch.log`, which nothing reads. The one channel that emails suppressed itself in exactly this case: `gmia-fetcher-health.py` sent only when `alerts["failing"] or warning or recovered`, and it probes at 04:30 BJT, 45 minutes after the 03:45 pipeline — so on 2026-09-06, when acadian-asset fetched 0 articles and the site answered normally by 04:30, every probe passed and the email was suppressed. Five months of `fetch.log` hold exactly one acadian zero and nobody saw it. A zero-article fetch is now its own send condition (`should_email`), reported in its own section rather than mixed into probe rows, because the two are different measurements taken 45 minutes apart. `pipeline_zero_fetches` never raises: extra reporting must not be able to fail the health check it is bolted onto. `tests/test_wrapper_synthesis_heartbeat.py` (added 9-08) drives `scripts/wrapper-fetcher-synthesis.sh` in a sandbox repo and asserts that a session finding no targets still writes a heartbeat. `scripts/gmia_liveness_audit.py` judges the weekly synthesis job by the freshest heartbeat, but the wrapper's no-target path did `exit 0` before reaching the heartbeat block — whose own comment says "always write one line per session, even when agent did nothing" — so a healthy run with nothing to do was indistinguishable from a job that never fired. The 2026-09-05 session logged two lines and wrote nothing, and the 09-08 audit reported "no session in 9d"; it had happened before (08-22, same two lines) and stayed invisible because the heartbeat tests were writing fake rows into the production history on every pytest run. `SYNTHESIS_LOCK_FILE` is overridable so the test does not take the production lock — a test holding it would make a real weekly run bail as "another instance is running", i.e. suppress the job it checks. **The suite no longer touches the network.** `tests/conftest.py`'s `_no_network` autouse fixture blocks `socket.connect` for every test that is not marked `live` or `nightly`: `tests/test_unit_discover.py` had never patched `discover_entrypoints._call_llm`, so two of its cases made real, billed gemini-2.5-pro requests on every run — 8.5s and 6.0s, about half the suite's wall clock — while asserting nothing about the reply, and the daily `gmia-nightly-test` and `gmia-auto-promote` crons each run pytest. Blocking centrally rather than mocking case by case, because patching each call site is the shape that gets forgotten. Runtime went 22.9s → 10s. (Two corrections to earlier figures, both stated as measured rather than reconciled: the 934 recorded on 9-06 was 2 low — commit `b8558f1` collects 936 on a clean extract, and no verified explanation for that gap was found; and commit `9b23451`'s message says "942 -> 949" when the measured count after it is 958, because its 9 new `tests/test_backfill_themes.py` cases were not counted. That commit was already pushed and this repo receives automated cron commits, so it was left unamended rather than force-pushed.) **Model chain (set 9-07):** `[gpt-5.6-luna, gpt-4.1-mini, gemini-2.5-flash]`. The last tier moved off gemini-2.5-pro because it only runs when BOTH OpenAI tiers fail — insurance, not a running cost — and flash is ~4x cheaper per call ($0.30/$2.50 vs $1.25/$10) while keeping the chain from being single-provider. Measured on 3 real articles: 3/3 parseable, no truncation at 12000, themes populated, and the same usage fields as pro (`promptTokenCount + candidatesTokenCount + thoughtsTokenCount == totalTokenCount`), so the accounting needed no new case — flash thinks hard too, 1550 thinking tokens against 198 of visible content on one article, all billed as output. The Gemini model id had been hard-coded into the request URL, so pointing the chain at a cheaper Gemini model would have kept calling the expensive one while logging the cheap one's name; a test now pins the URL to the model passed. The leading two tiers were decided by a measured 30-article bake-off — content 25-26,639 chars, run with the theme-allowlist prompt. All three parse 30/30; monthly cost at 369 articles is luna $0.36, gpt-4.1-mini $0.57, gemini-2.5-pro $8.76. Luna leads on behaviour rather than price: it states when the source text does not support an answer instead of writing around the gap (in one case flagging that a cited Morningstar figure was not present in the text), and it files articles under the theme they are actually about — gpt-4.1-mini put "AI/Tech" first on 14 of 30, including "Global Fixed Income Team Views & Outlook" and "Tight bond spreads mean no stone unturned", which `publish.py` clusters by `themes[0]` and would have filed under AI. Each OpenAI model carries its own request shape in `_OPENAI_PARAMS` (verified live: gpt-5.6-luna rejects `max_tokens` and rejects `temperature: 0.4`; an unlisted model raises rather than guessing, since one shape for both would make the leading tier fail every call and fall through — indistinguishable from a working fallback). `claude-sonnet-4-6` left the chain because `_load_api_keys` reads only `~/.stock-monitor.env` and `~/.secrets.env` and the Anthropic key lives in `~/.openclaw/.env`, so that tier could never run: the documented three-tier fallback had been two tiers. **`tests/conftest.py` keeps the suite out of production data in two layers (added 9-07), because the same mistake was found three times in two days and every instance was a list someone forgot to extend.** The PREVENT layer (`_redirect_production_writes`, autouse) points the module-level paths we know about — `analyze_articles.USAGE_LOG_FILE`, `fetch_articles.INSPECTION_STATE_FILE` — at one session-scoped throwaway directory. The DETECT layer (`_no_production_writes`, session-scoped) fingerprints every file under `logs/ config/ data/ content/ pending_profiles/` by size and mtime and fails the run if any changed; it enumerates nothing, imports nothing, and survives a renamed constant or a path added later. It was mutation-tested: a probe test writing one file into `logs/` turns it red naming that file. The three instances it exists for: (1) the 9-06 token accounting made `tests/test_unit_analyze.py` append 2 fake rows per run to `logs/analyze-usage.jsonl` (8 rows accumulated in a day, removed); (2) `scripts/write_session_heartbeat.py` pinned its output into a **default argument** (`history_path: Path = HISTORY_FILE`), bound once at def time, so `main()` ignored every `monkeypatch.setattr(hb, "HISTORY_FILE", ...)` and both its `main()` tests wrote into the real `logs/fetcher-synthesis-history.jsonl` from 2026-05-08 on — 551 of 561 heartbeat rows were test artefacts, identifiable as `(3,0)`/`(2,2)` pairs seconds apart, and `scripts/gmia_liveness_audit.py` treats a fresh heartbeat as beating a lock-bail marker while synthesis itself runs weekly, so a pytest run (the daily 02:30 BJT `gmia-auto-promote` cron runs one) forged the signal that says the weekly job is alive. Fixed with a None sentinel; the 551 rows were removed, leaving 10 real heartbeats whose newest is 2026-08-30; (3) four tests in `tests/test_unit_fetch_articles.py` reached `record_quality_metrics` and wrote fabricated source ids (`dup-test-source`, `sorted-test-source`, `unsorted-test-source` — the latter two shipped by `cad8970` the previous day) into the real `config/inspection_state.json`, while `TestRecordQualityMetrics` three classes above patched the same constant correctly. `TestTestsNeverTouchProductionLog` reads the production path from `conftest.PRODUCTION_PATHS`, captured before patching, rather than rebuilding it by hand: a hand-copied literal keeps passing after the constant is renamed, which is the canary reporting all-clear while the suite writes to the new file. `TestDateSortedGuard` (added 9-06 `cad8970`) covers the listing-order contract: acadian-asset.com's insight listing defaults to "Sort By: Relevance", not Date, and for about a year relevance happened to put the newest posts first, so taking the first 10 DOM cards worked — on 2026-09-02 it stopped and nine 2016-2023 archive pieces were ingested as new, summarised, and published, while August's two real posts fell off the visible page entirely. The staleness WARN reported only "most recent article 98d old", which reads as "the site stopped publishing" — the opposite of what happened — and nothing anywhere saw "we just ingested a 2016 article". The sort is URL-addressable as `?sortby=date-desc` (the obvious guesses `?sort=` / `?sortType=` / `#sort=` / `?sort_by=` are all silently ignored and return relevance order without erroring), so the fix is config-only; sources that pin a chronological sort declare `"date_sorted": true` and `fetch_source` refuses the WHOLE batch when the returned dates are not non-increasing, because a partial accept is exactly how archive pieces get in. Refusing surfaces as "0 articles", which `scripts/gmia-fetcher-health.py` already reports as FAIL, so a future silent revert to relevance sort cannot go quiet. `tests/test_analyze_usage_log.py` (added 9-06 `5778c8e`/`bcbb7bd`) covers token accounting, which did not exist: every call returned its counts and `_analyze_with_fallback` stashed them in `parsed["_usage"]`, which was never read, persisted or logged, so the cost of summarisation had no answer at all. Rows land in `logs/analyze-usage.jsonl` and are booked at the HTTP boundary rather than the success boundary — a response that fails to parse burned the same tokens as one that parsed, and gemini-2.5-pro 503s then retries every few days — while an exception books nothing, since a zero row there would be fabricated. The tests pin two traps found by probing the live API rather than trusting the field names in the code's comments: the three providers spell the same numbers differently (`promptTokenCount`/`candidatesTokenCount`, `prompt_tokens`/`completion_tokens`, `input_tokens`/`output_tokens`) so a normaliser with an `or 0` fallback would book an unrecognised payload as free — unknown stays None; and gemini-2.5-pro is a reasoning model whose `thoughtsTokenCount` (305 of 339 output tokens in a live probe) is billed as output, so counting `candidatesTokenCount` alone understated every row ~9x while looking plausible. The provider's own total is recorded alongside so the next unmapped field self-announces as `input + output != provider_total_tokens` in the data. An AST guard holds the last mile — `article_id` defaults to `""` so the chain stays callable from tests, which makes the `main()` call site forgettable in a way that logs fine and is useless. `tests/test_no_hardcoded_hosts.py` (added 9-03 `bb4198b`/`32a32b1`) is a fleet-wide guard: it parses every fetcher in `FETCHERS` and `CONTENT_FETCHERS` to an AST and fails on any URL literal that actually executes, and on any `source.get("<config field>", "<literal>")`. AST rather than a text regex because docstrings legitimately quote URLs — `fetch_researchaffiliates` documents both the old and the new host — and comments do too. It closes the 2026-08-21 Research Affiliates rename: config was pointed at syzygyassetmanagement.com and the fetcher still returned 0 articles with no error, because every href was rebuilt on the hardcoded old host and then dropped by `_validate_hostname`. Only that one fetcher was converted at the time; the sweep found 42 more base-URL literals (including hosts embedded in regexes for ares, brookfield and schroders, where a rename stops matching rather than failing) plus 40 `expected_hostname` fallbacks, two of which had already drifted from config. All now derive from the URL the fetcher was handed via `fetch_articles._site_base`. Two genuine exceptions are allowlisted on `(module, function, literal)` — the sitemaps.org XML namespace URI and the third-party Coveo API host — keyed on all three because `fetch_goehring_rozencwajg` holds that spec constant next to two site URLs that must follow config, and a function-level exemption would have excused them; a further test fails if an allowlist entry stops matching a live literal. `test_config_consistency.test_every_source_declares_expected_hostname` (added with it) makes reading the field directly safe: `tests/test_sanity.py` already asserted it but is `pytest.mark.live` and so deselected in the default run. `TestFetchMsciResearch::test_uses_domcontentloaded_not_networkidle` (added 9-03 `41dbf51`) came out of the fleet-wide sweep that followed the troweprice fix: 9 listing and 4 content fetchers relied on networkidle, all 13 reached it when measured live, and msci-research was the one worth changing anyway — it idles on every load but only after ~19s (9 loads: 18.7-19.5s) while its cards are in the DOM at ~8-9s, leaving a healthy run at ~25s against the 30s goto timeout. networkidle and domcontentloaded returned identical (url, date) pairs over 5 live runs, so unlike fetch_researchaffiliates this source is not waiting on async hydration. `TestFetchTroweprice::test_uses_domcontentloaded_not_networkidle` and `TestTrowepriceContentFetcher` (added 9-03 `7651bda`) pin `wait_until="domcontentloaded"` on both T. Rowe Price fetchers: every troweprice.com page load fires ~75 CSP-violation reports to api.public.troweprice.com and 1-3 of them never settle, so `networkidle` was reached in only 3 of 6 instrumented loads while the article cards were in the DOM after ~3s — the listing probe timed out on both attempts on 2026-09-03 and reported the source FAIL, and the content fetcher hit the same timeout on 2026-08-30. Same class as the fetch_pimco (7-19) and Wellington fixes, so the guard asserts the readiness condition rather than the fetch result, which a healthy network would pass either way. Fifteen cases across `tests/test_synthesis_summary_email.py` / `tests/test_synthesis_history.py` / `tests/test_backfill_failed_synthesis.py` (added 8-30) cover the fetcher-synthesis digest's honesty fixes. The failure reason shown in the email came from the candidate's `notes` field, which `gmia-trial-manager.py`, `guard_candidate_status.py`, `discover_candidate_entrypoints.py` and `status_util.py` all write — so a failed row would have displayed a discovery-time or trial-time note as this run's cause; the reason now travels in a dedicated `synthesis_failure_reason` candidate field that only the synthesis run and `backfill_failed_synthesis.py` write, is carried into the jsonl as `failure_reason`, and `notes` is rendered only as an explicitly disclaimed fallback. History rows are dated in BJT rather than UTC, because the Sunday 02:00 BJT run happens at 18:00 UTC Saturday and the UTC date labelled it Saturday — the date printed in the digest could not be grepped in the log it cites (one existing row, mfs-investment-management, was migrated; reconcile dedup registers both the UTC and BJT date of every row so the switch cannot append a duplicate, which would silently inflate the rate). The digest also now states when the rolling success rate comes from a history with zero recorded failures — 14/14 success at the time of the fix, which cannot be told apart from a failure path that never writes a row — and flags targets that returned neither a success nor a failure, which previously vanished silently unless the session recorded nothing at all. Reason text is clipped with a visible marker and the backfill reason is kept inside the 120-char cell budget so it never loses its trailing log pointer. `test_stored_article_hosts_are_declared` plus `test_base_url_follows_config_host_not_hardcoded` (added 8-21 `d6267b0`/`ffdd9a1`) cover the Research Affiliates → Syzygy Asset Management rename: researchaffiliates.com now 302s every listing path to its own homepage while article URLs land on syzygyassetmanagement.com, and pointing the config at the new host still returned 0 articles because `fetch_researchaffiliates` rebuilt every relative href on a hardcoded old-host base and then dropped it in `_validate_hostname` — the base now comes from `source["url"]`. The stored-host test closes the matching blind spot on disk: a source may accumulate hosts over its life (rename, acquisition, newsletter move), so `sources.json` declares them in `historical_hostnames` and every `articles.jsonl` row must sit on a declared host — `test_no_cross_source_contamination` never covered this because it is `nightly`-marked and inspects a live fetch rather than stored data. Six `ops_problems` / `ops_recovered` cases (added 8-18) pin the liveness audit's recovery semantics: only a job's LATEST in-window ops-status run decides its verdict, chosen by timestamp rather than file order, so an already-fixed failure is reported as `recovered` instead of re-raising a BAIL every day until it ages out of the 3-day window — the 8-17 playwright incident kept alarming on 8-18 although gmia-nightly-test and gmia-fetcher-health were both green again hours later. `TestFetchMsciResearch` plus five `parse_date` cases (added 8-16 `6d8208b`) cover the AP-style abbreviated month fix — MSCI moved its listing dates from "August 14, 2026" to "Aug. 14, 2026" in 2026-08 and `strptime`'s `%b` accepts "Aug" but not "Aug.", so every article on the source parsed to `date=None`; the health check only caught it on 08-13, when the last July-dated article rolled out of the 10-item listing window and `max(parsed dates)` went empty. `parse_date` now normalises a leading abbreviated month plus period to the three-letter form, including AP's four-letter "Sept." which `%b` never accepts — stripping the period alone would have left a second outage waiting in September — and a negative case pins that "Foo. 3, 2026" still returns None. `TestFetchVerdad` / `TestVerdadFetcher` (rewritten 8-15 `607a776`) cover the Verdad re-point to its Mailchimp newsletter feed — a full-site audit (sitemap `lastmod` all <= 2026-05-05, Squarespace `publishOn` newest 2026-05-04) confirmed the website froze while the newsletter never missed a Monday, so the fetcher reads the campaign-archive RSS and a regression case pins that it hits `rss_url` rather than the dead page `url`. The content fetcher must select `.mcnTextContent`: mailchi.mp pages are table-based email HTML with no `<p>` tags at all, so the old `article p` selector fell through `_normalize_html`'s fallbacks to whole-page `get_text()` and dragged the language picker and unsubscribe footer into the body — a test asserts that chrome stays out. `TestFetchRothschildCoAm` (added 8-05 `4cb451b`) covers the Rothschild & Co Asset Management listing-page redesign — the site swapped `li.filterFormListingItem` markup for `li.listing-item.article-listing-item`/`roths-article-card-*` between 8-03 and 8-05, and a regression case pins that the new full-ISO `datetime` attribute (`2026-07-09T23:00:00Z`) must be parsed directly rather than falling back to the page's ambiguous DD/MM display text, which `parse_date()`'s `%m/%d/%Y`-first ordering previously misread as MM/DD. `TestFetchMetlifeIm` / `TestMetLifeIMFetcher` (rewritten 8-03 `543cc4d`) cover the MetLife IM re-point after MIM retired `investments.metlife.com` — the fetcher now drives off the AEM listing servlet (day-precise `publishedDate`; the new sitemap's `<lastmod>` is a migration crawl stamp shared by 38 of 46 URLs, and article pages lost their JSON-LD), and both the article and content fetchers must send the `culture` + `disclaimer` cookie pair or every URL 302s to the country/role interstitial and the source silently returns 0 articles. A dedicated test also pins the deliberate absence of a `main p` content fallback, which would let 260-400 char PDF-teaser blurbs through as if they were the research. `tests/test_unit_guard_candidate_status.py` (added 7-15) covers `guard_candidate_status.py`'s `PIPELINE_STATUS_TRANSITIONS` check — a status transition to a pipeline-owned value (screened/screen_failed/visitable/inaccessible) is only accepted when the corresponding script-owned freshness field (`last_screened_at` / `last_validated_at`) actually advanced in that session, distinguishing a legitimate Phase 1 pipeline run from the discovery agent hand-editing the status field to match; `promoted` is never accepted this way since only `gmia-trial-manager.py` sets it. `tests/test_config_consistency.py`'s cadence check (added 7-14) now detects sources where 80%+ of articles carry only month/year date precision (e.g. troweprice, kkr — real sites, not a scraper bug) and judges staleness in whole calendar months instead of days, since day-gap math manufactures fake 14-60d gaps purely from calendar-boundary rounding. `tests/test_unit_status_util.py` / `tests/test_unit_detect_stalled_candidates.py` / `tests/test_unit_backfill_status_since.py` (added 7-15) cover the `status_since` stall-detection feature — every candidate status write now stamps `status_since` only on an actual change (via `status_util.set_status()`), `detect_stalled_candidates.py` auto-routes candidates stuck too long (seed with a confirmed `research_url` → `discovered`; `screen_failed` → `inaccessible` + `needs_playwright`), and `backfill_status_since.py` one-time-backfilled the field for all pre-existing candidates via a `last_*_at` fallback chain. `test_pool_articles_carry_data_seq_in_global_date_order` / `test_timeline_populate_sorts_pool_by_seq` (added 7-06 `d77d369`) cover the timeline chronological-order fix — each pool article carries `data-seq` (its global date-descending rank) and the timeline view sorts by it before appending, because `returnArticlesToPool()` scrambles pool DOM order after any Themes/Funds hydration (regression showed the timeline as fund-grouped blocks, e.g. all PGIM first, instead of a global date feed). `test_probe_respects_per_source_content_probe_top_n_override` / `test_probe_without_override_still_defaults_to_top_3` (added 7-04) cover the fetcher-health per-source `content_probe_top_n` override in `sources.json` — added for matthews-asia (probes top 6 instead of the global default top 3) after 2 consecutive daily false-positive FAILs from short teaser/video pages ahead of the next full-length article. metlife-im took the same override at 8 on 8-26 (`ca16ca4`) for the same class of false positive from the other direction: its quarterly "Review and Outlook" pieces are PDF-teaser pages that the content fetcher deliberately returns None for (see the `main p` fallback note above), and the Q2 batch shipped five of them (08-10 x2, 08-14, 08-19, 08-20) — once a newly listed one pushed the last full-length article to position 4, all of top 3 were teasers and the source FAILed while the production pipeline, which fetches all 10 articles rather than a top-N sample, kept storing its content normally. Raising the override costs nothing on a healthy run: the probe stops at the first article that yields content. `TestFetchSourceIntraRunDedup` (added 7-03) covers intra-run article dedup in `fetch_source` — a fetcher returning the same URL twice in one result list (wellington did) previously appended duplicate jsonl rows / duplicate HTML ids. `tests/test_candidate_probe_pool.py` (added 7-02 `0416089`) covers the fetcher-health `--include-validated` probe pool — candidates in an active trial are excluded (the trial exercises real fetching daily, incl. Playwright CF bypass), so trial-period Cloudflare 403s no longer fail the nightly health run. Contract tests enforce `sources.json` stays in sync with the `FETCHERS` / `CONTENT_FETCHERS` dispatcher dicts and `BADGE_COLORS` palette, so adding a new production source without wiring the full pipeline fails fast at pytest time. Consistency tests (`tests/test_config_consistency.py`) additionally guard against frequency-vs-observed-cadence drift, validated-candidate URL invariants, and fund-profile coverage (every `sources.json` id must have a profile in `publish._FUND_PROFILES` or `pending_profiles/<id>.json`). `TestOlderArticleFolding` covers the `data-age` recency split (90d boundary inclusive after the 5-29 `f561da8` Show-older fold tightening, no-date defaults to recent, page-level toggle button render/suppress). `tests/test_validate_pending_profile.py` (added 5-29 `13c51f0`) enforces auto-graduate evidence gates — `aum_source` / `founded_source` URL/domain citations, AUM↔desc_zh currency consistency, $10M-$20T magnitude bounds. `tests/test_trial_auth_gate.py` (added 6-02) covers trial-manager auth-gate detection — cookie content-gates (Akamai-style 302→200 redirects) are retried with a persistent-cookie session instead of being misclassified as JS-only / LOW QUALITY, plus day-1 fail-fast for fetcher-less candidates and inconclusive/aborted email labeling. `tests/test_apply_refresh.py`, `tests/test_validate_refresh.py`, `tests/test_send_refresh_summary.py`, and `tests/test_profile_static_facts.py` (added 6-08) cover the monthly profile-refresh path — change_log-driven entry updates that leave every unchanged field byte-identical, per-changed-field evidence gating (drops `validate_profile`'s unconditional source demands), and a static-fact guard that fails if a hand-audited founder/claim (e.g. GMO's `Eyk van Otterloo`) is ever regressed.

```bash
python3 -m pytest tests/ -q
```

## Requirements

- Python 3.12+
- `requests`, `beautifulsoup4`, `playwright`, `feedparser`
- Chromium browser (`playwright install chromium`)

## Data

- `data/articles.jsonl` — 74 articles (metadata + summaries), gitignored
- `content/*.txt` — fetched article content files
- `config/sources.json` — source configuration
- `config/entrypoints.json` — verified entrypoint URLs per source
- `config/inspection_state.json` — fetch quality metrics for anomaly detection
